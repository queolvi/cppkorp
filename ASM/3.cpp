#include<iostream>

long long foo(long long eax) {

long long unsigned rdi;
long long unsigned rbp_24 = rdi;
int rbp_4;
long long rdx;
long long unsigned rcx;
long long unsigned rax = rdi;
rax += 9;

if(rax = 18) {
	 rbp_4 = 1;
       if(rbp_24 != 0) {
	rcx = rbp_24;
        rdx = std::abs(7378697629483820647);
        rax = rcx;
        eax *= rdx;
        rdx >>= 2;
        rax = rcx;
        rax >>= 63;
        rdx -= rax;
        rax = rdx;
        rax <<= 2;
        rax += rdx;
        rax += rax;
        rcx -= rax;
        rdx = rcx;
        eax = rbp_4;
        eax *= rdx;
        rbp_4 = eax;
        rcx = rbp_24;
        rdx = std::abs(7378697629483820647);
        rax = rcx;
        eax *= rdx;
        rax = rdx;
        rax >>= 2;
        rcx >>= 63;
        rdx = rcx;
        rax -= rdx;
        return rbp_24 = rax;


       } else {
       
	return eax = rbp_4;

       }	
}
return -1;
}

int main() {


	for(int i = -100; i < 100; ++i) {
		std::cout << i  << "\t" << foo(i) << "\n";
}

	return 0;
}
